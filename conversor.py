# Conversor

def strcode(code):
	if (code==""): 
		print("ERRO") 
	while (code!=EOF):
		note = code[:7]
		code = code[8:]
return note

def note_conv(note):
	int (note, 2) # Convert a binary string to a decimal int
		
		


